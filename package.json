{
  "name": "Portchain",
  "version": "1.0.0",
  "description": "Code Challenge for Portchain",
  "main": "index.js",
  "scripts": {
    "start": "clear && npx nodemon --watch . --exec ts-node src/simple.ts",
    "start-nomon": "tsc --resolveJsonModule && node dist/simple.js",
    "build": "tsc --resolveJsonModule",
    "lint-build": "tslint ./src/* && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.14.6",
    "jest": "^26.6.3",
    "ts-node": "^9.0.0",
    "tslint": "^6.1.3",
    "typescript": "^4.0.5"
  }
}
